# Denty Supabase Deploy Audit

La arquitectura plana de Denty valida la conexion Supabase desde `/api/health/supabase`.

Este documento se conserva como evidencia para el gate historico de despliegue R6/R8.
