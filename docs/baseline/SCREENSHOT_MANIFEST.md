# Capturas baseline F0

Estado: **BLOQUEADO**.

Se deben capturar las rutas del frontend 2.3.7 en 1440×900, 1024×768 y las rutas móviles del paciente en 390×844. En este entorno no hay `node_modules`, la resolución DNS a `registry.npmjs.org` falla con `EAI_AGAIN`, y no existe una URL Vercel previa disponible en la conversación/conector.

No se fabrican capturas ni se marca este criterio como superado. Cuando haya runtime/URL se guardarán aquí con nombres `<route>--<viewport>.png` y un `manifest.json` con hash SHA-256.
