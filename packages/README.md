# Packages

Espacio reservado para extraer librerias compartidas de Denty cuando el dominio,
los contratos API o la UI se separen de `apps/web`.

Por ahora la aplicacion desplegable vive en `apps/web` para mantener el deploy de
Vercel sencillo y estable.