export function resolveRelativeDate(text:string,now=new Date()):Date|null{const n=text.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase();const d=new Date(now);d.setHours(0,0,0,0);if(n==="hoy")return d;if(n==="manana"){d.setDate(d.getDate()+1);return d}if(n==="pasado manana"){d.setDate(d.getDate()+2);return d}const days=["domingo","lunes","martes","miercoles","jueves","viernes","sabado"],idx=days.indexOf(n);if(idx>=0){let add=(idx-d.getDay()+7)%7;if(add===0)add=7;d.setDate(d.getDate()+add);return d}const m=n.match(/^(\d{1,2})[\/-](\d{1,2})(?:[\/-](\d{2,4}))?$/);if(m){const year=m[3]?Number(m[3].length===2?`20${m[3]}`:m[3]):now.getFullYear();return new Date(year,Number(m[2])-1,Number(m[1]))}return null}
